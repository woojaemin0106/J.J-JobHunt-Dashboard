Check UI consistency and demo readiness:
- Verify design token usage consistency (buttons, inputs, cards, spacing).
- Verify accessibility baseline (focus visible, contrast, clear action labels).
- Verify interview flow clarity (login -> applications -> notes/statistics -> logout).

Output should include a short "UI consistency checklist" with pass/fail/unknown.
